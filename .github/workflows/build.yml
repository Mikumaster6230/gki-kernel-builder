name: Build GKI 5.4 Kernel with KernelSU

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: "Kernel version"
        required: false
        default: "5.4"
      device_name:
        description: "Device name"
        required: false
        default: "meizu18s"

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up environment variables
        run: |
          echo "KERNEL_VERSION=${{ github.event.inputs.kernel_version || '5.4' }}" >> $GITHUB_ENV
          echo "DEVICE=${{ github.event.inputs.device_name || 'meizu18s' }}" >> $GITHUB_ENV
          echo "BUILD_TIME=$(date +%Y%m%d-%H%M%S)" >> $GITHUB_ENV

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            build-essential flex bison bc git make rsync \
            libssl-dev device-tree-compiler cpio \
            python3 python3-pip wget curl ca-certificates \
            clang-14 lld-14 llvm-14 \
            gcc-aarch64-linux-gnu

      - name: Setup toolchain symlinks
        run: |
          sudo ln -sf /usr/bin/clang-14 /usr/local/bin/clang
          sudo ln -sf /usr/bin/clang++-14 /usr/local/bin/clang++
          sudo ln -sf /usr/bin/ld.lld-14 /usr/local/bin/ld.lld
          sudo ln -sf /usr/bin/lld-14 /usr/local/bin/lld
          sudo ln -sf /usr/bin/llvm-ar-14 /usr/local/bin/llvm-ar
          sudo ln -sf /usr/bin/llvm-nm-14 /usr/local/bin/llvm-nm
          sudo ln -sf /usr/bin/llvm-strip-14 /usr/local/bin/llvm-strip
          sudo ln -sf /usr/bin/llvm-objcopy-14 /usr/local/bin/llvm-objcopy
          sudo ln -sf /usr/bin/llvm-objdump-14 /usr/local/bin/llvm-objdump
          sudo ln -sf /usr/bin/llvm-readelf-14 /usr/local/bin/llvm-readelf

      - name: Cache kernel source
        id: cache-kernel
        uses: actions/cache@v4
        with:
          path: ~/kernel
          key: gki-5.4-source-v10

      - name: Clone GKI 5.4 kernel source
        if: steps.cache-kernel.outputs.cache-hit != 'true'
        run: |
          git clone --depth=1 --single-branch \
            -b android12-5.4 \
            https://github.com/aosp-mirror/kernel_common.git \
            ~/kernel || \
          git clone --depth=1 --single-branch \
            -b deprecated/android12-5.4 \
            https://github.com/aosp-mirror/kernel_common.git \
            ~/kernel

      - name: Download and integrate KernelSU
        run: |
          cd ~/kernel
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -

      - name: Fix KernelSU 5.4 compat
        run: |
          cd ~/kernel
          # Create kernel 5.4 compatibility header
          cat > include/linux/ksu_54_compat.h << 'CEOF'
          #ifndef _KSU_54_COMPAT_H
          #define _KSU_54_COMPAT_H
          #include <linux/uaccess.h>
          #include <linux/kernel.h>
          /* ===== pgtable.h shim (5.8+ API) ===== */
          #include <asm/pgtable.h>
          #ifndef pte_index
          static inline unsigned long pte_index(unsigned long a) { return (a >> PAGE_SHIFT) & (PTRS_PER_PTE - 1); }
          #endif
          #ifndef pmd_index
          static inline unsigned long pmd_index(unsigned long a) { return (a >> PMD_SHIFT) & (PTRS_PER_PMD - 1); }
          #endif
          #ifndef pud_index
          static inline unsigned long pud_index(unsigned long a) { return (a >> PUD_SHIFT) & (PTRS_PER_PUD - 1); }
          #endif
          #ifndef p4d_index
          static inline unsigned long p4d_index(unsigned long a) { return (a >> P4D_SHIFT) & (PTRS_PER_P4D - 1); }
          #endif
          /* ===== strncpy_from_user_nofault wrapper (5.10+ API, backport for 5.4) ===== */
          static inline long strncpy_from_user_nofault(char *dst, const void __user *unsafe_addr, long count)
          {
            long ret;
            pagefault_disable();
            ret = strncpy_from_user(dst, (const char __user *)unsafe_addr, count);
            pagefault_enable();
            return ret;
          }
          /* ===== fallthrough macro (5.4 doesn't have it) ===== */
          #ifndef fallthrough
          #define fallthrough do {} while (0)  /* fallthrough */
          #endif
          #endif /* _KSU_54_COMPAT_H */
          CEOF
          # Create pgtable.h that includes our compat header
          cat > include/linux/pgtable.h << 'PEOF'
          #ifndef _LINUX_PGTABLE_H
          #define _LINUX_PGTABLE_H
          #include <asm/pgtable.h>
          #ifndef pte_index
          static inline unsigned long pte_index(unsigned long a) { return (a >> PAGE_SHIFT) & (PTRS_PER_PTE - 1); }
          #endif
          #ifndef pmd_index
          static inline unsigned long pmd_index(unsigned long a) { return (a >> PMD_SHIFT) & (PTRS_PER_PMD - 1); }
          #endif
          #ifndef pud_index
          static inline unsigned long pud_index(unsigned long a) { return (a >> PUD_SHIFT) & (PTRS_PER_PUD - 1); }
          #endif
          #ifndef p4d_index
          static inline unsigned long p4d_index(unsigned long a) { return (a >> P4D_SHIFT) & (PTRS_PER_P4D - 1); }
          #endif
          #endif /* _LINUX_PGTABLE_H */
          PEOF
          # Add compat include to ALL KernelSU C source files (not just sucompat.c)
          for src in $(find drivers/kernelsu -name '*.c' -type f); do
            if ! grep -q 'ksu_54_compat' "$src" 2>/dev/null; then
              sed -i '1i#include <linux/ksu_54_compat.h>' "$src"
            fi
          done
          # Also add to kernel su headers used in main kernel tree
          for hdr in $(find drivers/kernelsu -name '*.h' -type f); do
            if grep -q 'strncpy_from_user_nofault\|pgtable\|pte_index\|pmd_index' "$hdr" 2>/dev/null; then
              if ! grep -q 'ksu_54_compat' "$hdr" 2>/dev/null; then
                sed -i '1i#include <linux/ksu_54_compat.h>' "$hdr"
              fi
            fi
          done
          echo "KernelSU 5.4 compat fixes applied to all source files"

      - name: Configure kernel
        run: |
          cd ~/kernel
          make O=out ARCH=arm64 mrproper 2>/dev/null || true
          make O=out ARCH=arm64 gki_defconfig
          scripts/config --file out/.config --enable KPROBES
          scripts/config --file out/.config --enable HAVE_KPROBES
          scripts/config --file out/.config --enable KPROBE_EVENTS
          scripts/config --file out/.config --enable OVERLAY_FS
          scripts/config --file out/.config --enable MODULES
          scripts/config --file out/.config --enable MODULE_UNLOAD
          scripts/config --file out/.config --disable DEBUG_INFO
          # Resolve ALL new config options non-interactively (CC=clang is critical)
          make O=out ARCH=arm64 CC=clang LD=ld.lld CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

      - name: Build kernel
        run: |
          cd ~/kernel
          # Prevent make from re-triggering interactive config sync
          export KCONFIG_NOSILENTUPDATE=1
          make O=out \
            ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            STRIP=llvm-strip \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            READELF=llvm-readelf \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            -j$(nproc --all) 2>&1 | tee build.log

      - name: Check build result
        run: |
          cd ~/kernel
          if [ -f out/arch/arm64/boot/Image ]; then
            echo "SUCCESS: Image found!"
            ls -lh out/arch/arm64/boot/Image*
          else
            echo "FAIL: Image not found"
            tail -80 build.log
            exit 1
          fi

      - name: Package artifacts
        run: |
          mkdir -p ~/artifacts/modules
          cd ~/kernel
          cp out/arch/arm64/boot/Image ~/artifacts/Image 2>/dev/null || true
          cp out/arch/arm64/boot/Image.gz ~/artifacts/Image.gz 2>/dev/null || true
          find out/ -name "*.ko" -exec cp {} ~/artifacts/modules/ \; 2>/dev/null || true
          cp out/.config ~/artifacts/kernel_config 2>/dev/null || true
          cd ~/artifacts
          tar -czf GKI-${KERNEL_VERSION}-${DEVICE}-${BUILD_TIME}.tar.gz *
          ls -lh ~/artifacts/

      - name: Upload kernel artifact
        uses: actions/upload-artifact@v4
        with:
          name: GKI-${{ env.KERNEL_VERSION }}-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
          path: ~/artifacts/GKI-*.tar.gz
          retention-days: 30

      - name: Build summary
        run: |
          echo "============================================"
          echo "  GKI ${{ env.KERNEL_VERSION }} Kernel Built"
          echo "============================================"